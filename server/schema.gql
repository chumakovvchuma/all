# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type Post {
  id: ID!
  text: String!
  author: User!
  comment: [Comment!]!
}

type Comment {
  id: ID!
  text: String!
  author: User!
  post: Post!
}

type User {
  id: ID!
  email: String!
  name: String!
  role: UserRole!
  comment: [Comment!]!
  post: [Post!]!
}

enum UserRole {
  AUTHOR
  ADMIN
}

type Login {
  email: String!
  password: String!
}

type Query {
  Login(email: String!): Login!
  Logins(skip: Int = 0, take: Int = 25): [Login!]!
  Post(id: String!): Post!
  Posts(skip: Int = 0, take: Int = 25): [Post!]!
  User(id: String!): User!
  Users(skip: Int = 0, take: Int = 25): [User!]!
  Comment(id: String!): Comment!
  Comments(skip: Int = 0, take: Int = 25): [Comment!]!
}

type Mutation {
  addLogin(newLoginData: NewLoginDTO!): Login!
  removeLogin(email: String!): Boolean!
  addPost(newPostData: NewPostDTO!): Post!
  removePost(id: String!): Boolean!
  addUser(newUserData: NewUserInput!): User!
  removeUser(id: String!): Boolean!
  addComment(newCommentData: NewCommentDTO!): Comment!
  removeComment(id: String!): Boolean!
}

input NewLoginDTO {
  email: String!
  password: String!
}

input NewPostDTO {
  text: String!
}

input NewUserInput {
  text: String!
}

input NewCommentDTO {
  text: String!
}

type Subscription {
  PostAdded: Post!
  UserAdded: User!
  CommentAdded: Comment!
}
